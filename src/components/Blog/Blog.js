import React from 'react';
import Header from '../Header/Header';

const Blog = () => {
    return (
        <div>
            <Header filters='invert(1)'/>
            <h1 className='text-center'>Blog area coming soon....</h1>
        </div>
    );
};

export default Blog;